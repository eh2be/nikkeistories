$(document).ready(function(){

  $('.single-item').slick({
    dots: true,
    autoplay: true,       // autoplay
    autoplaySpeed: 5000,  // autoplay
    infinite: true,       // fade
    speed: 1000,          // fade
    fade: true,           // fade
    slide: 'div',         // fade
    cssEase: 'ease'     // fade
  });

});